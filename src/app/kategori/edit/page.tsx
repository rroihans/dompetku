import { KategoriDetailClient } from "@/components/kategori/kategori-detail-client"

// Static export compatible - no dynamic route segments here.
// Everything is handled via search params in the client component.

export default function Page() {
    return <KategoriDetailClient />
}
